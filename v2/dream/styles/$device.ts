// Сгенерировано esm-styles v2 из tokens/<mode>.device.ts —
// руками не редактировать. Перегенерация: npm run gen:v2 (или watch:v2).
// Токен — обычная строка 'var(--…)'; значения режимов видны при hover.

export default {
  radius: {
    /** mobile `3px` · desktop `4px` */
    sm: 'var(--device-radius-sm)',
    /** mobile `8px` · desktop `12px` */
    xl: 'var(--device-radius-xl)',
  },
  spacing: {
    /** mobile `0.75rem` · desktop `1rem` */
    md: 'var(--device-spacing-md)',
    /** mobile `1.25rem` · desktop `2rem` */
    lg: 'var(--device-spacing-lg)',
  },
}
