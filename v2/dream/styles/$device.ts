// Воображаемый сгенерированный справочник (из media.device в конфиге).

export default {
  radius: {
    /** desktop `4px` · mobile `3px` */
    sm: 'var(--device-radius-sm)',
    /** desktop `12px` · mobile `8px` */
    xl: 'var(--device-radius-xl)',
  },
  spacing: {
    /** desktop `1rem` · mobile `0.75rem` */
    md: 'var(--device-spacing-md)',
    /** desktop `2rem` · mobile `1.25rem` */
    lg: 'var(--device-spacing-lg)',
  },
}
