// Сгенерировано esm-styles v2 из tokens/<mode>.theme.ts —
// руками не редактировать. Перегенерация: npm run gen:v2 (или watch:v2).
// Токен — обычная строка 'var(--…)'; значения режимов видны при hover.

export default {
  paper: {
    /** light `#ffffff` · dark `#16161a` · twilight `#1a1430` */
    bright: 'var(--theme-paper-bright)',
    /** light `#f4f1ea` · dark `#1e1e24` · twilight `#241b3e` */
    tinted: 'var(--theme-paper-tinted)',
  },
  ink: {
    /** light `#1a1a1a` · dark `#ececf1` · twilight `#ececf1` */
    strong: 'var(--theme-ink-strong)',
    /** light `#6b6b6b` · dark `#9a9aa3` · twilight `#9a9aa3` */
    mild: 'var(--theme-ink-mild)',
    /** light `#dcdcd4` · dark `#2c2c33` · twilight `#2c2c33` */
    faint: 'var(--theme-ink-faint)',
  },
  /** light `#c24d2c` · dark `#e07a5f` · twilight `#9f7aea` */
  accent: 'var(--theme-accent)',
}
